$schema: https://json-schema.org/draft/2020-12/schema
type: object
required: [schema_version, public_contract_changed, roadmap_reconciliation_requested]
properties:
  schema_version: {const: relay-v3-status-publication-signals}
  public_contract_changed: {type: boolean}
  roadmap_reconciliation_requested: {type: boolean}
additionalProperties: false
