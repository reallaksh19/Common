# Engineering relay bootstrap

For material engineering work, begin at `agents/relay/REPO_STATE.yaml`.

Do not infer active work from chat history, newest commits, newest issue comments, directory timestamps, or previous-agent prose. Read the referenced roadmap, confirm the computed executable frontier, then execute only the active EP.
