# Performance blueprint

When performance matters: identify the user-visible latency/throughput/memory concern, measure a reproducible baseline, define the EP threshold, avoid optimizing outside the measured path, re-measure after change, record environmental limitations, and treat unmeasured performance claims as hypotheses rather than evidence.