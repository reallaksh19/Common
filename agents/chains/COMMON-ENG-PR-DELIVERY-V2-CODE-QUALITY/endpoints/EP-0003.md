# EP-0003 — Final review-ready concurrency policy

CHAIN_ID: COMMON-ENG-PR-DELIVERY-V2-CODE-QUALITY
LEG_ID: LEG-001
ENDPOINT_ID: EP-0003
PREVIOUS_ENDPOINT: EP-0002
CUSTODY_EPOCH: 3

CREATED_AT: 2026-08-28T01:34:00Z
ENDPOINT_REASON: READY_FOR_NEXT_LEG

TASK / ISSUE: Owner-requested v2 code-quality plus multi-agent concurrency upgrade
PR: #22
BRANCH: engineering-pr-delivery-v2-code-quality

CHECKPOINT_HEAD: 22d1b3885992fcb6f5683ba3329232229e2838f1
MAIN_HEAD_OBSERVED: 10d667ce715bb52e1f73035c6fa326db77d0f9dd
MERGE_BASE: 10d667ce715bb52e1f73035c6fa326db77d0f9dd

STATE: READY_FOR_NEXT_LEG

### Mission

Seal the review-ready canonical v2 update: modular code-quality gates plus concurrency-safe chain-local relay state for independent multi-agent workstreams.

### This leg completed

- canonical new-chain state is per-chain `ACTIVE.md` plus immutable `endpoints/**`;
- endpoint IDs are chain-local and keyed by `(CHAIN_ID, ENDPOINT_ID)`;
- `CUSTODY_EPOCH` plus exact prior repository version/blob provides stale-write protection;
- shared `agents/agentchain.md` is derived/legacy navigation and is not modified in the final PR diff;
- legacy v2 shared-index history and validator remain available;
- canonical validator is self-contained;
- six concurrency fixtures passed locally;
- canonical ACTIVE examples use machine-readable `PR: <number-or-PENDING>` rather than Markdown `#` tokens;
- code-quality thresholds and modularity/authority rules remain first-class.

### Currently in progress

PR #22 is ready for final metadata synchronization and owner review/merge decision.

### Remaining work

- update PR title/body to the final expanded scope and validation evidence;
- re-check exact changed-file list/live main;
- merge only if explicitly authorized;
- downstream repositories can migrate new chains after Common merge.

### Exact next action

Refresh PR #22 description and exact diff/live-main evidence, then await explicit owner merge authorization.

### Known / proven

The final architecture removes the relay bookkeeping collision between independent WRC/LAFEA/LoadCalc chains while retaining fail-closed same-chain custody and semantic overlap checks.

### Not proven

Advanced_Analysis has not yet exercised three simultaneous canonical production chains; full historical aggregate suite from a cloned checkout remains unavailable because container DNS cannot resolve GitHub.

### NOT_RUN

- downstream concurrent production-chain pilot — NOT_RUN;
- cloned-checkout full aggregate suite — NOT_RUN due transport limitation.

### Active hypothesis

The final PR is backward-compatible for legacy evidence and scalable for new concurrent chains without weakening engineering authority controls.

### Falsifier

Any final diff that requires shared dashboard mutation for ordinary canonical progression, breaks legacy recovery, accepts same-chain divergent custody, or turns relay separation into automatic engineering independence falsifies readiness.

### Protected invariants

Repository baton, immutable history, chain-local stale-write protection, independent qualification, source/oracle integrity, explicit NOT_RUN, code-quality ownership rules, and owner-only merge.

### Do not redo

Do not restore global endpoint IDs or a mandatory shared active pointer.

### Do not change

Historical legacy endpoints, v1 rollback implementation, workflows, qualification thresholds, engineering data/formulas/oracles, unrelated skills.

### Expected next-leg files / domains

PR metadata and later chain-local merge/closure endpoint only unless review finds a concrete defect.

### Inputs

Owner code-quality and concurrent-agent requests; Common current v2 and CodingRules.

### Benchmarks

- cross-chain `EP-0001` reuse: PASS;
- independent WRC/LAFEA/LoadCalc paths: PASS;
- sequential same-chain epochs: PASS;
- stale epoch rejection: PASS;
- divergent same-chain successors rejection: PASS;
- shared-path coordination detection: PASS.

### Common / governing documents

`AGENTS.md`, canonical `SKILL.md`, `agentchain-schema.md`, `agentchain-template.md`, `chain-concurrency.md`, `multi-agent-coordination.md`, `code-quality.md`.

### Authoritative sources

Current owner instructions and Common base `10d667ce715bb52e1f73035c6fa326db77d0f9dd`.

### Production paths

NONE — governance/skill implementation only.

### Validation / test paths

`validate_chain_store.py`, `detect_chain_overlap.py`, `render_agentchain_dashboard.py`, `check_relay.py`, `self_test_chain_concurrency.py`, `self_test.py`, and legacy `validate_agentchain.py`.

### Changed during this leg

Root policy, v2 skill/references/scripts, and this chain's canonical `agents/chains/**` relay evidence only.

### Validation summary

New concurrency layer: PASS 6/6 targeted fixtures. Direct Git clone/full historical aggregate: NOT_RUN due DNS transport. Live main remained unchanged through EP-0002 grounding.

### Open risks / questions

Downstream migration should be new-chain-first; semantic authority overlap still requires substantive engineering review.

### Next-agent qualification

QUALIFICATION_BASIS_HEAD: 22d1b3885992fcb6f5683ba3329232229e2838f1
QUESTION_SET_ID: QS-COMMON-V2-CODE-QUALITY-0003
QUESTION_SET_STATUS: CURRENT

#### Q1 — Production Trace

Repository anchors: root `AGENTS.md`, `SKILL.md`, `agents/chains/**`, `validate_chain_store.py`.
Required evidence: trace WRC and LAFEA creating independent `EP-0001` endpoints and advancing only their own ACTIVE files.
Falsifier / decisive observation: either chain must update a shared current pointer.

#### Q2 — Current Unresolved Problem / Failure Isolation

Repository anchors: concurrency self-test and validator.
Prediction: cross-chain duplicate local IDs pass; stale epoch/divergent same-chain lineage fail.
Required evidence: reproduce fixtures and isolate first incorrect invariant on mismatch.
Falsifier: opposite behavior for either case.

#### Q3 — Authority / Invariant

Repository anchors: multi-agent coordination and overlap detector.
Required authority trace: separate relay ownership from engineering/source/benchmark/release independence.
Protected invariant: different chain directories do not automatically imply SAFE engineering concurrency.
Invalid shortcut to reject: no relay-file overlap means no authority overlap.

#### Q4 — Independent Validation

Repository anchors: legacy shared-index history/validator and canonical store/validator.
Independent oracle/reference required: prove legacy evidence remains readable while new chains no longer depend on shared dashboard freshness.
Units/sign/tolerance requirements: NOT_APPLICABLE.
Required evidence: exact path/validator availability and unchanged historical custody.

#### Q5 — Next Contribution / Minimal Patch

Repository anchors: PR #22 exact diff and live main.
Smallest legitimate change: PR metadata only unless a concrete policy/script defect is found.
Expected changed files/domains: relay closure metadata after merge authorization.
Protected unchanged files/domains: historical evidence, workflows, unrelated skills, engineering formula/source/oracle authority.
Validation required: exact changed-file audit and live-main re-ground.
Rollback/falsifier boundary: revert this architecture rather than weaken stale-write or compatibility guarantees.
