# EP-0001 — Code-quality and multi-agent concurrency upgrade

CHAIN_ID: COMMON-ENG-PR-DELIVERY-V2-CODE-QUALITY
LEG_ID: LEG-001
ENDPOINT_ID: EP-0001
PREVIOUS_ENDPOINT: NONE — chain start
CUSTODY_EPOCH: 1

CREATED_AT: 2026-08-28T01:20:00Z
ENDPOINT_REASON: CHAIN_START

TASK / ISSUE: Owner-requested v2 code-quality plus multi-agent concurrency upgrade
PR: #22
BRANCH: engineering-pr-delivery-v2-code-quality

CHECKPOINT_HEAD: 8ff6e05efb3dd38039097179b601b0061bdd0ca5
MAIN_HEAD_OBSERVED: 10d667ce715bb52e1f73035c6fa326db77d0f9dd
MERGE_BASE: 10d667ce715bb52e1f73035c6fa326db77d0f9dd

STATE: READY_FOR_NEXT_LEG

### Mission

Promote modular code-quality rules and concurrency-safe chain-local relay state into canonical `engineering-pr-delivery-v2`, so independent WRC, LAFEA, LoadCalc, and other agents do not contend on one shared relay file.

### This leg completed

- added `references/code-quality.md` and R18-R25 for modular maintainability;
- made new canonical relay state `agents/chains/<CHAIN_ID>/ACTIVE.md`;
- made immutable endpoint identity chain-local as `(CHAIN_ID, ENDPOINT_ID)`;
- added `CUSTODY_EPOCH` compare-and-swap semantics for same-chain stale-write protection;
- added canonical chain-store validation, derived dashboard rendering, and concurrency self-tests;
- updated overlap detection to operate on per-chain active state while retaining legacy shared-index compatibility;
- updated root `AGENTS.md`, skill schema/model/templates, crash recovery, and multi-agent policy;
- removed the temporary globally unique `EP-CQ-0001` workaround rather than preserving the wrong global endpoint-ID model;
- restored legacy `agents/agentchain.md` to its pre-PR historical content so this new chain does not require shared-index mutation.

### Currently in progress

PR #22 is open and unmerged. Final exact-diff review and executable validation remain before owner merge authorization.

### Remaining work

- run/inspect canonical chain-store and concurrency validations against this branch;
- confirm PR #22 contains only intended skill/governance/relay changes;
- confirm live main has not materially moved;
- update PR description to the expanded owner-approved scope;
- merge only after explicit owner authorization.

### Exact next action

Validate the canonical chain store and concurrency self-test, audit PR #22 exact changed files against live main, then await owner merge authorization.

### Known / proven

- endpoint IDs are now defined chain-local, so WRC/LAFEA/LoadCalc can each use `EP-0001` without ambiguity;
- canonical chains write only their own `ACTIVE.md`, eliminating routine cross-chain relay-file conflicts;
- same-chain custody is guarded by an explicit monotonically increasing epoch plus exact prior repository version/blob discipline;
- the shared `agents/agentchain.md` is retained as legacy/derived navigation rather than canonical write authority;
- existing legacy v2 histories are preserved and not mass-rewritten;
- code-size thresholds remain design-review triggers rather than blind hard limits.

### Not proven

- Advanced_Analysis has not yet migrated its existing active/legacy chains to `agents/chains/**`;
- no claim is made that chain-local relay state eliminates real engineering overlap between independent chains;
- full local aggregate suite result on the final branch is not yet recorded at this endpoint.

### NOT_RUN

- downstream Advanced_Analysis migration/pilot of concurrent WRC + LAFEA + LoadCalc chains — NOT_RUN;
- final full aggregate self-test on this exact branch head — NOT_RUN at endpoint creation.

### Active hypothesis

Chain-local current-state files eliminate bookkeeping collisions between independent engineering agents while custody epochs fail closed for true same-chain races, without weakening authority/benchmark/source overlap controls.

### Falsifier

The hypothesis is falsified if independent chains still must mutate one shared relay file to advance, if `EP-0001` cannot coexist across different chains, if same-chain divergent successors can both become current without reconciliation, or if the migration invalidates legacy historical evidence.

### Protected invariants

- repository state remains the baton;
- endpoints remain immutable;
- qualification remains READ_ONLY until independent verdict;
- `NOT_RUN` stays distinct from PASS;
- real authority/path/benchmark/dependency overlap still requires coordination;
- owner-only merge remains unchanged;
- code organization cannot create engineering authority.

### Do not redo

Do not restore repository-global endpoint identity or make `agents/agentchain.md` mandatory for every canonical endpoint advance. Do not mass-rewrite legacy endpoint history.

### Do not change

- legacy v1 implementation except explicit compatibility documentation;
- engineering qualification thresholds;
- source/oracle/tolerance authority;
- unrelated Common skills;
- workflows.

### Expected next-leg files / domains

- v2 skill/references/scripts only if validation finds a defect;
- this chain's `ACTIVE.md` and later immutable endpoint for relay closure;
- PR #22 metadata.

### Inputs

- owner request for `<300 lines` and modular code practice;
- owner multi-agent scenario: concurrent WRC, LAFEA, and LoadCalc agents in one repository;
- current Common v2 implementation and `CodingRules.md`.

### Benchmarks

- structural concurrency fixture: WRC/LAFEA/LoadCalc each use `EP-0001` on disjoint paths and must validate;
- negative fixture: same-chain divergent successors must fail;
- negative fixture: stale `ACTIVE.md` custody epoch must fail;
- negative fixture: real shared production-path overlap must require coordination.

### Common / governing documents

- `AGENTS.md`;
- `CodingRules.md`;
- `skills/engineering-pr-delivery-v2/SKILL.md`;
- `references/agentchain-schema.md`;
- `references/chain-concurrency.md`;
- `references/code-quality.md`.

### Authoritative sources

- current owner instructions for code-quality and concurrent-agent scaling;
- live Common canonical v2 policy at PR base `10d667ce715bb52e1f73035c6fa326db77d0f9dd`.

### Production paths

NONE — no application engineering production code changes. Governed skill paths are under `skills/engineering-pr-delivery-v2/**` and root `AGENTS.md`.

### Validation / test paths

- `skills/engineering-pr-delivery-v2/scripts/validate_chain_store.py`;
- `skills/engineering-pr-delivery-v2/scripts/detect_chain_overlap.py`;
- `skills/engineering-pr-delivery-v2/scripts/render_agentchain_dashboard.py`;
- `skills/engineering-pr-delivery-v2/scripts/self_test_chain_concurrency.py`;
- `skills/engineering-pr-delivery-v2/scripts/self_test.py`;
- legacy `validate_agentchain.py` for compatibility.

### Changed during this leg

- root `AGENTS.md`;
- canonical `SKILL.md`;
- code-quality and concurrency references;
- relay schema/model/templates/recovery/coordination references;
- canonical chain-store/overlap/dashboard/check scripts and self-tests;
- new chain-local relay evidence under `agents/chains/COMMON-ENG-PR-DELIVERY-V2-CODE-QUALITY/**`.

### Validation summary

- architecture source inspection: PASS — canonical writes are now chain-local;
- legacy compatibility design: PASS_BY_INSPECTION — old validator and historical files remain;
- code-quality source comparison: PASS_BY_INSPECTION;
- final executable branch validation: NOT_RUN at endpoint creation.

### Open risks / questions

- downstream repositories should migrate new chains first rather than mass-converting active legacy history;
- same-chain compare-and-swap relies on repository version/blob or Git conflict semantics being respected by the acting tool;
- semantic authority overlap remains an expert/repository-evidence question, not something a path-only detector can fully prove.

### Next-agent qualification

QUALIFICATION_BASIS_HEAD: 8ff6e05efb3dd38039097179b601b0061bdd0ca5
QUESTION_SET_ID: QS-COMMON-V2-CODE-QUALITY-0001
QUESTION_SET_STATUS: CURRENT

#### Q1 — Production Trace

Repository anchors: root `AGENTS.md`, `SKILL.md`, `chain-concurrency.md`, `validate_chain_store.py`.
Required evidence: trace how a new WRC chain creates `ACTIVE.md`/`EP-0001` and how a parallel LAFEA chain can create its own `EP-0001` without shared relay mutation.
Falsifier / decisive observation: normal canonical progression still requires writing one shared repo-wide pointer.

#### Q2 — Current Unresolved Problem / Failure Isolation

Repository anchors: `validate_chain_store.py` and `self_test_chain_concurrency.py`.
Prediction: chain-local duplicate endpoint IDs pass across different chains; same-chain divergent successors and stale epochs fail.
Required evidence: reproduce the positive/negative fixtures and identify the first failing invariant if behavior differs.
Falsifier: either cross-chain `EP-0001` is rejected or same-chain divergence is accepted.

#### Q3 — Authority / Invariant

Repository anchors: `multi-agent-coordination.md`, `detect_chain_overlap.py`, endpoint authority/source inventories.
Required authority trace: show why WRC/LAFEA can be relay-independent yet still require coordination if they touch shared units, source authority, benchmarks, or common production paths.
Protected invariant: relay separation must not imply engineering-authority independence.
Invalid shortcut to reject: `different ACTIVE.md => automatically SAFE`.

#### Q4 — Independent Validation

Repository anchors: legacy `agents/agentchain.md`, old `validate_agentchain.py`, new `validate_chain_store.py`.
Independent oracle/reference required: verify legacy history remains readable/unchanged while canonical new chains validate separately.
Units/sign/tolerance requirements: NOT_APPLICABLE to governance mechanics.
Required evidence: demonstrate no mass rewrite of immutable legacy endpoints and no loss of legacy validator path.

#### Q5 — Next Contribution / Minimal Patch

Repository anchors: PR #22 exact changed-file list and live main.
Smallest legitimate change: fix only a concrete concurrency/code-quality policy or validator defect; otherwise no further material code is needed.
Expected changed files/domains: v2 policy/scripts plus chain-local relay closure metadata.
Protected unchanged files/domains: unrelated skills, workflows, engineering formulas/data/oracles, legacy endpoint history.
Validation required: canonical chain validator, concurrency suite, overlap detector fixture, exact diff audit.
Rollback/falsifier boundary: if chain-local state cannot coexist with legacy compatibility or fail-closed same-chain custody, revert the concurrency migration rather than weaken either invariant.
