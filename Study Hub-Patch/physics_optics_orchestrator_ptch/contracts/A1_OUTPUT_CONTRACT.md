# A1 Output Contract

Required zip contents:

```text
output/
  topic.json
  assets-manifest.json
  question-set-map.json
  schema-notes.md
  pages/
    *.json
```

Rules:
- Must define stable ids/slugs
- Must include Asset 1 and Asset 2 page maps
- Must include question bundle definitions
- Must not contain generic placeholder text
