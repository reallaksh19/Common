# PHY-V2-03 Physics Study Synthesis

This directory owns **what the synthetic learner should actively study** for the Physics acceptance case. It consumes merged canonical Physics refs and the merged PHY-V2-02 descriptive state. It does not own diagnosis, teaching choreography, scheduling, publication, human review, or PR #156 comparison.

Expected treatment:

```text
reference frame                     VERIFY_ONLY / READY
model selection                     USE_AS_ENTRY_POINT / READY
state-variable meaning              USE_AS_ENTRY_POINT / READY
projectile decomposition            VERIFY_ONLY / PROBE_FIRST
state propagation                   ACTIVE_STUDY / REPAIR_BEFORE
signed displacement                 ACTIVE_STUDY / PROBE_FIRST
shared arithmetic execution         EXTERNAL DEPENDENCY / REPAIR_IN_UNIT
```

A probe does not erase demonstrated capability. A state-handoff failure does not become `KINEMATICS=WEAK`.
