# PHY-V2-01 Canonical Physics Reasoning Authority

This directory freezes learner-independent Physics reasoning semantics for the first V2 Physics proof.

It owns canonical system/frame/state/model/transition/sign/validation/verification meaning. It does **not** own learner diagnosis, Study Synthesis treatment, teaching choreography, publication layout, human review, or comparative validation.

The central acceptance invariant is:

```text
valid projectile / phase reasoning upstream
+ invalid later state handoff
!= KINEMATICS_WEAK
```

`PHY-MOTION-PROPAGATE-STATE` is first-class. Its nine checkpoints require the terminal state of one phase to become the initial state of the next for variables continuous across the boundary, unless a modeled discontinuity explicitly changes that state variable.

Shared arithmetic/algebra execution appears only through external dependency interfaces. PR #156 is not a producer input.
