# MATH-V2-03 — Mathematics Study Synthesis proof

Tracking: #205. Parent roadmap: #197. Shared authority: #175 / V2-04 #191.

This package proves the Mathematics **what-to-study** boundary using public synthetic interfaces only.

```text
LearningGoalRequest + CanonicalTargetScope
+ CanonicalKnowledgeSet interface
+ ResearchLearnerView interface
+ Math StudySynthesisPolicy
→ MathLearnerStudyModel
```

The StudyModel preserves demonstrated modelling/setup capability while localizing algebra repair, routing unresolved coordinate semantics to a probe, and retaining verification as an explicit study obligation.

It does **not** contain lesson sequence, example order, hint ladders, fading, practice counts, scheduling, page/layout directives, renderer primitives, raw learner attempts, or benchmark references.

Temporary interface fixtures are `PUBLIC_SYNTHETIC` and `INTERFACE_FIXTURE_ONLY`. They carry stable IDs and descriptive state needed for conformance; they are not a second canonical registry or learner authority.
