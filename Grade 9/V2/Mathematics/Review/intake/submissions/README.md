# Human review submissions

`real/` is intentionally empty of JSON submissions until actual reviewer identities are authorized in `reviewer_authorization.real.json` and those reviewers submit evidence bound to the exact candidate SHA.

`test-only/` contains synthetic fixtures for executable contract tests. They are never release evidence.
