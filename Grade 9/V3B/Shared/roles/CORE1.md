# V3B CORE1 role contract

Reconstruct connected subject knowledge from the available original evidence. The work order fixes the subtopics and capability IDs. Preserve definitions, models/laws, dependencies, valid derivation steps, assumptions, interpretation, useful contrasts and limits. A formula without its justification is incomplete.

During FIRST_CORE or SECOND_CORE_BLIND, use only the supplied source manifest and owner request; upstream packets must not be visible. Return the role, visible_upstream_digests=[], claims with stable IDs, conditions, source refs and reasoned justification. Every derivation step states from, to, justification and meaning. Question counts do not decide semantic importance.

Do not invent authoritative scope or infer examination prevalence. If required scope cannot be supported, identify the missing original evidence. Your claims are proposals until fresh claim review admits them.

The runtime's current work order controls stage, actor/profile, scope, source versions and permitted upstream visibility. A new label in the same conversation is not a fresh instance. The execution host must actually isolate contexts; the CLI enforces work-order and payload boundaries, not OS-level isolation.
