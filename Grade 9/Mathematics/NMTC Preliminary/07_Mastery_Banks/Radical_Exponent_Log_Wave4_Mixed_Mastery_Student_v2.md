# Radicals, Exponents & Logarithmic Transformations — Mixed Mastery & Transfer v2

`ISSUE_AUTHORITY: #45`

`WAVE: 4 — MIXED_MASTERY_AND_TRANSFER`

`AUDIENCE: STUDENT`

`STATUS: INTERNAL_STUDENT_SOURCE`

All items in this file are `AUTHOR_CREATED_FOUNDATION` or `AUTHOR_CREATED_TRANSFER`. They are **not** presented as NMTC previous-year questions.

This is deliberately unlabelled by chapter. Your job is to decide the representation and first move yourself.

For every item, preserve any domain/sign/non-zero/range condition that the mathematics requires.

Recognition codes available from the First-Step Reference:

`CB HS PR EM EN EV ER RQ ZR RI LD LV LS LA LI DR QC`

---

# A. Recognition only — 20 prompts

Do **not** solve. Write only the best first-move code. A second code is allowed only when checking is genuinely part of the first decision.

1. Simplify `sqrt(50)+sqrt(8)-sqrt(2)`.
2. Simplify `sqrt(17-4sqrt(15))`.
3. Simplify `sqrt((2x-7)^2)` for real `x`.
4. Interpret and simplify the structure of `81^(-3/4)`.
5. Solve `32^x=8^(x+1)`.
6. Solve `4^x-7*2^x+12=0`.
7. Solve `9^x-7*6^x+10*4^x=0`.
8. Solve `sqrt(x+5)=x-1`.
9. A proposed solution divides `(x-3)(x+2)=0` by `x-3` immediately.
10. If `u+1/u=5`, find `u^6+1/u^6`.
11. Convert `log_7 49=2` into exponent language.
12. Solve `(log_3 x)^2-4log_3 x+3=0`.
13. Solve `log_2 x-5sqrt(log_2 x)+4=0`.
14. Positive `x,y` satisfy `log_9 x=log_3 y`.
15. Evaluate exactly `16^(log_2 3)`.
16. After squaring a radical equation, two algebraic candidates remain.
17. A printed key gives a value that makes an original logarithm argument zero.
18. A real cube-root equation is to be cubed on both sides. Decide whether the transformation is reversible.
19. A student reads `5^-2` as a negative number.
20. Solve `16^x-10*4^x+9=0`.

---

# B. First useful line — 12 prompts

Do not finish the problem. Write only the first mathematically useful line, including a domain/range/non-zero condition when it belongs in that first line.

1. Simplify `sqrt(72)-sqrt(18)+sqrt(8)`.
2. Simplify `sqrt(29-12sqrt(5))`.
3. Simplify `sqrt((3x-4)^2)` and state when it equals `3x-4`.
4. Rewrite `125^(-2/3)` so the exponent meaning is explicit.
5. Solve `64^x=8^(x+2)`.
6. Solve `9^x-13*3^x+36=0`.
7. Solve `16^x-10*12^x+9*9^x=0`.
8. Solve `sqrt(x+6)=x-2`.
9. Solve `(x-3)(x+5)=0` without losing a zero-factor case.
10. If `t+1/t=4`, begin finding `t^5+1/t^5` without solving for `t`.
11. Solve `(log_5 x)^2-6log_5 x+5=0`.
12. Positive `x,y` satisfy `log_4 x=log_2 y`; write the equivalent algebraic relation with its domain.

---

# C. Mixed solve & transfer — 18 items

For each item:

1. write the first move before calculation;
2. solve exactly unless the problem asks for a conclusion rather than a number;
3. include original-domain / sign / range / zero-case checking whenever relevant.

## 1
Simplify

`(sqrt(200)-sqrt(32)+sqrt(8))/sqrt(2)`.

## 2
Simplify

`sqrt(29-12sqrt(5))`.

## 3
Evaluate exactly

`(11+6sqrt(2))^(3/2)-(11-6sqrt(2))^(3/2)`.

## 4
Evaluate exactly

`125^(-2/3)`.

## 5
Solve

`32^x=8^(x+2)`.

## 6
Solve

`9^x-13*3^x+36=0`.

## 7
Solve

`16^x-10*12^x+9*9^x=0`.

## 8
Solve over the reals

`sqrt(x+6)=x-2`.

Your solution must state the restriction needed before squaring and must reject any inadmissible algebraic root.

## 9
Solve over the reals

`sqrt(x+4)=2sqrt(x-5)`.

State the original domain before transforming.

## 10
Solve

`(x-2)(x+5)=0`.

Then explain in one sentence why dividing first by `x-2` would be unsafe.

## 11
If

`x+1/x=4`,

find

`x^5+1/x^5`.

Do not solve explicitly for `x`.

## 12
If

`x+1/x=4`,

is the value of

`x^2-1/x^2`

uniquely determined? Give the complete conclusion.

## 13
Solve

`(log_2 x)^2-5log_2 x+4=0`.

## 14
Solve

`log_3 x-4sqrt(log_3 x)+3=0`.

Carry the range restriction created by your substitution.

## 15
Positive real numbers `x,y` satisfy

`log_9 x=log_3 y`

and

`x-y=20`.

Find `x+y`.

## 16
Evaluate exactly

`27^(log_3 5)`.

Do not use decimal logarithms.

## 17
Solve

`log_2(x-1)=log_2(7-x)`.

Write the common original domain before equating arguments.

## 18
Solve over the reals

`sqrt((x-1)^2)=3`.

Your reasoning must distinguish principal-root notation from solving a squared equation.

---

# D. WHY-NOT contrasts — 6 items

A correct answer must say whether the tempting move is **invalid** or merely **inferior**, and give the structural reason.

1. Why not simplify `sqrt(a+b)` as `sqrt(a)+sqrt(b)`?
2. Why not interpret `a^-2` as `-a^2`?
3. For `8^x=4^(x+1)`, why not begin by taking logarithms?
4. For `sqrt(x+6)=x-2`, why not square immediately without writing any restriction?
5. For `(x-2)(x+7)=0`, why not divide both sides by `x-2` and continue?
6. For `log_2 x-5sqrt(log_2 x)+4=0`, why is `t=log_2 x` a poorer substitution than `u=sqrt(log_2 x)`?

---

# E. Domain / reversibility checkpoint

The following Wave-4 items explicitly test domain, extraneous-root or admissibility custody:

- B3, B8, B9, B12;
- C8, C9, C10, C12, C14, C15, C17, C18;
- D4, D5, D6.

This exceeds the Issue #45 minimum of four such checks.

---

# F. Student self-check after completion

Before looking at the key, ask:

1. Did I name a first move before calculating?
2. Did I choose a representation because of structure rather than chapter memory?
3. Did I mark a risky transformation as `<=>` or `=>` when relevant?
4. Did I preserve `t>0`, `u>=0`, log arguments `>0`, and zero cases?
5. Did I avoid solving an underlying variable when an invariant was enough?
6. Did I distinguish an invalid method from a merely inefficient one?

`ANSWER_DIAGNOSTIC_KEY: separate Wave-4 file`

`PDF_RENDER_QA: NOT_RUN — Wave 5`
