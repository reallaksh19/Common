# P0 Algebra — Inequalities / Bounds Recognition Lab v1

## Purpose

Recognition only. Do **not** solve.

Suggested internal target: 20 items in 6 minutes after learning. This is a training target, not an official NMTC timing claim.

Choose one primary code:

`BD / AM / CY / CS / ZZ / DR / AV / RI / IC / DB / QC`

## Student sheet

1. `ab=9`, `a,b>0`; asks maximum of `a+b`.
2. `x+y=24`, `x,y>0`; asks maximum `xy`.
3. `xyz=27`, positive; asks minimum `x+y+z`.
4. `1/a+16/b=1`; asks least `a+b`.
5. `x^2-12x+40`; asks minimum.
6. `(p-3)^2+(q+7)^2=0`; target is large powers of `p,q`.
7. quadratic with parameter `m`; asks when real roots exist.
8. quadratic with parameter; asks when roots are equal.
9. `|x-8|<5`.
10. `|2x+1|>=7`.
11. `4/|x-3|>2`; asks integer count.
12. `(x-5)/(x+1)>=0`.
13. `((x-2)(x+4))/(x-7)<0`.
14. two inequalities are solved; problem asks sum of natural solutions.
15. expression contains `7-4sin t`; asks minimum.
16. four positive roots have product 16 and sum 8.
17. positive integers have fixed sum; AM-GM equality would require half-integers.
18. supplied key says “maximum = 6” but an escaping family makes target unbounded.
19. lower bound is proved, but equality conditions violate `x` integer.
20. real solution interval contains a point where original denominator is zero.

## Key

1 `BD`
2 `AM`
3 `AM`
4 `CY`
5 `CS`
6 `ZZ`
7 `DR`
8 `DR`
9 `AV`
10 `AV`
11 `AV` (then `IC`)
12 `RI`
13 `RI`
14 `IC`
15 `DB`
16 `AM`
17 `AM` with equality-feasibility check
18 `QC` / `BD`
19 `AM` with feasibility check
20 `IC` / domain check

## Diagnostic bands

- `18–20`: recognition adopted.
- `15–17`: targeted first-line remediation.
- `<15`: return to Concept Book contrasts before more solving.

## Critical failures

Any of these requires remediation even with high total score:

- Q1 classified as AM-GM maximum without boundedness;
- Q11 ignores denominator zero;
- Q18 accepts supplied key;
- Q19 assumes equality is attained;
- Q20 filters integers without original-domain check.
