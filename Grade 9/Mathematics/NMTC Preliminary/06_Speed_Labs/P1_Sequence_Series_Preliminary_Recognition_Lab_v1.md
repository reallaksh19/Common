# P1 Sequence & Series — Recognition Lab v1

## Rule

Do **not** solve. Choose the best first-move code.

Codes:

`TS` term/sum, `AP`, `GP`, `HI` high-index GP, `WS` weighted sum, `RR` reciprocal recurrence, `SH` shift recurrence, `FR` functional recurrence, `IG` infinite GP, `RV` reverse from `S_n`, `TP` telescoping, `FD` finite differences, `QC` source check.

1. “Find the 40th term” of a constant-difference list.
2. “Find the sum of the first 40 terms” of the same list.
3. `a_25/a_20` in a GP.
4. `Σ k(3k+2)`.
5. `a_{n+1}=a_n/(1+2a_n)`.
6. `a_{n+1}=3a_n-4`.
7. `a_{m+n}=a_m+a_n+mn` and target `a_16`.
8. Infinite geometric sum with unknown `a,r`.
9. Formula for `S_n`; target `a_n`.
10. `Σ 1/[k(k+1)]`.
11. `3,8,15,24,35,...`.
12. Two selected GP terms with indices differing by 12.
13. Sum of squares of an infinite GP is also given.
14. `Σ 1/(sqrt(k)+sqrt(k+1))`.
15. A sequence has constant second differences.
16. Historical GP wording gives one result but the answer key matches a different term relation.
17. `a_7=31,a_19=79` in an AP; need `d`.
18. `a_4=10,a_9=320`; need `a_30/a_25`.
19. `Σ(n²+n)`.
20. `S_n=n³+2n`; find nth term.

## Key

1 `AP` (with `TS` awareness)
2 `TS` then `AP`
3 `HI`
4 `WS`
5 `RR`
6 `SH`
7 `FR`
8 `IG`
9 `RV`
10 `TP`
11 `FD`
12 `HI`
13 `IG`
14 `TP`
15 `FD`
16 `QC`
17 `AP`
18 `HI`
19 `WS`
20 `RV`

## Internal target

- 17/20 correct: recognition adopted.
- 14–16: usable but slow/unstable.
- <14: return to First-Step cards before timed solving.