# NMTC Bhaskara Preliminary — Author-Created Mock A

`AUTHOR_CREATED_TRANSFER — not an official AMTI paper`

## T24 current-like training profile

- 30 questions
- 120 minutes
- Q01–Q15: multiple choice
- Q16–Q30: numerical response
- training score: `+1` correct, `-0.5` incorrect, `0` blank

The timing/marking profile is an author-created training convention grounded to the qualified 2024 paper; it is not claimed as the universal NMTC format.

---

# Section A — Multiple Choice

## Q01
The roots of `x^2-7x+10=0` are `alpha,beta`. The value of `alpha^2+beta^2` is

A. 19  
B. 29  
C. 39  
D. 49

## Q02
`sqrt(5+2sqrt6)` equals

A. `sqrt3+sqrt2`  
B. `sqrt3-sqrt2`  
C. `2+sqrt6`  
D. `sqrt6-1`

## Q03
For `x>0`, the minimum value of `x+9/x` is

A. 3  
B. 6  
C. 9  
D. 12

## Q04
The remainder when `2^2026` is divided by `7` is

A. 1  
B. 2  
C. 4  
D. 6

## Q05
A circle has centre `O` and radius `5`. From an external point `P`, `OP=13`. The length of a tangent from `P` to the circle is

A. 8  
B. 10  
C. 12  
D. 18

## Q06
An arithmetic progression has third term `11` and seventh term `23`. The sum of its first ten terms is

A. 155  
B. 170  
C. 185  
D. 200

## Q07
How many four-digit even numbers can be formed from the digits `1,2,3,4,5` without repetition?

A. 24  
B. 36  
C. 48  
D. 60

## Q08
A triangle has side lengths `13,14,15`. The median to the side of length `14` is

A. `sqrt37`  
B. `2sqrt37`  
C. `sqrt74`  
D. `4sqrt37`

## Q09
A proof establishes `P(k) -> P(k+2)` for all positive integers `k`, but checks only `P(1)`. What has actually been covered?

A. all positive integers  
B. only even positive integers  
C. only odd positive integers  
D. no positive integers

## Q10
`floor(-2.3)` is

A. `-3`  
B. `-2`  
C. `2`  
D. `3`

## Q11
The remainder when `x^5` is divided by `x^2-1` is

A. `0`  
B. `1`  
C. `x`  
D. `x+1`

## Q12
If `x>2` and `log_2 x + log_2(x-2)=3`, then `x` equals

A. 3  
B. 4  
C. 6  
D. 8

## Q13
The least positive integer greater than `2` that leaves remainder `2` when divided by each of `5,7,9` is

A. 107  
B. 212  
C. 317  
D. 632

## Q14
Opposite angles of a cyclic quadrilateral are supplementary. If one angle is `112 degrees`, the opposite angle is

A. `58 degrees`  
B. `68 degrees`  
C. `78 degrees`  
D. `88 degrees`

## Q15
What is the least number of people needed to guarantee that two were born in the same month?

A. 12  
B. 13  
C. 24  
D. 25

---

# Section B — Numerical Response

Write only the requested numerical value.

## Q16
The roots of `x^2-5x+3=0` are `alpha,beta`. Find

`3(1/alpha + 1/beta)`.

## Q17
If `3^x = 27sqrt3`, find `2x`.

## Q18
How many integers satisfy `|x-4|<3`?

## Q19
Find the remainder when `7^100` is divided by `10`.

## Q20
How many positive integers `n` make `(n+5)/(n-1)` an integer?

## Q21
Find the sum of the first five terms of the GP `3,6,12,...`.

## Q22
Find the coefficient of `x^3` in `(1+x)^6`.

## Q23
From an external point `P`, `PA` is tangent to a circle. A secant from `P` meets the circle first at `B` and then at `C`. If `PB=4` and `PC=9`, find `PA`.

## Q24
In triangle `ABC`, altitude `AD` meets `BC` at `D`. If `BD=9` and `DC=4`, find `AB^2-AC^2`.

## Q25
In the standard induction proof of `1+3+5+...+(2n-1)=n^2`, what term is added when passing from `n=10` to `n=11`?

## Q26
How many integer values of `x` satisfy `floor(x/3)=2`?

## Q27
A quantity is increased by `20%` and then decreased by `20%`. If its initial value is `100`, find its final value.

## Q28
If `x^2=x+1`, then `x^5=ax+b`. Find `a+b`.

## Q29
Find the greatest positive integer that leaves the same remainder when dividing each of `58,100,142`.

## Q30
Find `floor(sqrt50)`.

---

## Student close-out record

After finishing, record separately:

- questions recognized immediately;
- questions where the first move was uncertain;
- questions left blank;
- questions changed during checking.

Do not inspect the teacher key until this record is complete.
