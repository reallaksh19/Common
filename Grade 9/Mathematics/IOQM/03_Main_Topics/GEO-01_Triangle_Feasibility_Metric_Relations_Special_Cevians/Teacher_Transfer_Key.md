# Teacher Transfer Key — GEO-01

Status: `TRANSFER_ITEMS_INDEPENDENTLY_SYNCHRONIZED_V1`

This key covers the eight prompts in the **Transfer Bank** section of `05_Practice_and_Transfer_Bank.md`.

1. **Triangle third-side -> quadrilateral diagonal.** Invariant: the candidate segment must satisfy strict triangle feasibility. A quadrilateral diagonal is simply the third side of two triangles, so intersect the two strict intervals.

2. **Side classification -> every-triple threshold.** Invariant: after sorting `a<=b<=c`, acuteness is controlled by `c^2<a^2+b^2`. The surface change adds an extremal-selection problem; the exact distinct/repetition semantics determine the legal worst triple.

3. **Midpoint median -> ratio-marked cevian.** Invariant: classify from givens. `BD=DC` earns median/Apollonius; a generic ratio `BD:DC` does not. For the changed surface, retain only the stated split and use a general or other directly earned metric relation.

4. **Explicit angle bisector -> misleading symmetric sketch.** Invariant: angle-bisector status is earned only by equal-angle data/proof. Appearance cannot substitute for `angle BAD=angle DAC`.

5. **Right-triangle altitude -> integer hypotenuse/perimeter.** Invariant: close the continuous right-triangle metric relation first (`h^2=pq`, projection identities, or the area relation). Only then apply integer factor/divisibility filtering.

6. **Exradius data -> side reconstruction.** Invariant: route through area to semiperimeter complements: `Delta=r_a(s-a)=r_b(s-b)=r_c(s-c)`, then reconstruct sides from `x=s-a`, `y=s-b`, `z=s-c`.

7. **Synthetic metric -> coordinate alternate.** Invariant: geometric hypotheses do not change with representation. Choose coordinates only if they reduce execution cost; preserve midpoint/perpendicular/ratio/angle-bisector information explicitly.

8. **Verified historical answer -> exact source-page/figure custody.** Invariant: answer verification and source custody are separate controls. A verified number does not authorize invented wording, marks or figures. For GEO-01 all eight exact printed pages are now visually closed and are text-only, so any learner schematic is author-created rather than a reproduced historical figure.

These transfer responses have been independently synchronized against the Assimilation Book, source-custody record and diagnostic hierarchy. This is static authoring QA, not classroom or psychometric validation.
