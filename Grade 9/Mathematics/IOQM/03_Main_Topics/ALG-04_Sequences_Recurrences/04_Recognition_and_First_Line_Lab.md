# ALG-04 — Recognition and First-Line Lab

Write only the first useful line or recognition statement.

1. `S_n=2n^2+3n`; asked for `a_n`.
2. `5,9,13,17,...`.
3. `3,12,48,192,...`.
4. 5-term moving sums are strictly increasing.
5. `a_{n+2}=5a_{n+1}-4a_n`; look for a recurrence for first differences.
6. `sum 1/[k(k+1)]`.
7. `sum 1/[(k-1)k]`.
8. A recurrence is defined from counting tilings of a board.
9. Asked for `a_1000` from a second-order recurrence.
10. `S_n` is given and the problem asks whether the sequence is increasing.

Recognition labels:
`TERM_FROM_SUM`, `AP`, `GP`, `WINDOW_DIFF`, `RECURRENCE_TRANSFORM`, `TELESCOPE`, `COMB_STATE_BRIDGE`.
