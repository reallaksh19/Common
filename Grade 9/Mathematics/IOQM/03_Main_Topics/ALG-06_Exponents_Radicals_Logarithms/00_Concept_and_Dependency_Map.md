# ALG-06 — Exponents, Radicals & Logarithms

Status: `DRAFT_AUTHORING_STARTED`
Issue: `#82`
Production base at kickoff: `grade9-ioqm-90q-corpus-v1@4b30638f984076d41998dcd2c68f4b3830a7d59c`

## Learner target
Grade 9 learner with about 50% prior knowledge. The governing sequence is:

**Normalize → state the domain → mark whether each transformation is reversible → solve → validate in the original condition.**

Manipulation shortcuts are never allowed to hide domain loss or candidate generation.

## Frozen prerequisite consumed
Provider: `IOQM-G9-ALG-01`

Exact locator:
`Grade 9/Mathematics/IOQM/03_Main_Topics/ALG-01_Identities_Transformations/Authoring/ALG01_Stable_Prerequisite_Interface_v1.md`

Provider status: `FROZEN_FOR_DOWNSTREAM_CONSUMPTION`.

Retrieve without reteaching:
- target-led representation choice;
- identity vs admissible-solution relation;
- equivalence vs implication-only transformation;
- zero-denominator / zero-divisor branch protection;
- candidate validation in the original condition.

ALG-01 explicitly does not export radical/log principal-value or full domain doctrine; ALG-06 owns that canon.

## Canonical ownership
ALG-06 owns:
- exponent normalization and common-base reduction;
- principal square roots and radical domains;
- conjugates and rationalising transformations;
- nested radicals;
- reversible and non-reversible transformations;
- logarithms as exponents;
- log-to-algebra conversion with base/argument restrictions;
- relevant integer/domain filtering.

## Hard mathematical constraint
For every squaring, root extraction or logarithmic transformation, the solution must state:
1. the active domain conditions;
2. whether the step is an equivalence (`iff`) or only an implication (`=>`);
3. which new candidates may have been introduced or lost;
4. the final check in the original equation whenever a one-way step occurred.

## Mandatory source correction
`IOQM-2025-Q28` must use the corrected nested-radical stem:

`sqrt(x - sqrt(x+a)) = sqrt(a) - y`

The defective earlier extraction is prohibited. The metadata-correction overlay must be cited in the source audit before promotion.

Second historical anchor: `IOQM-2023-Q02`, to be solved independently from exact source custody.

## Mandatory contrasts
1. square both sides vs preserve equivalence;
2. common-base reduction vs logarithm;
3. simple radical difference vs nested radical;
4. algebraically plausible candidate vs domain-valid solution.

## Microstreams
Each will receive one separate full A–P authoring interface:
1. exponents / normalization;
2. radicals / conjugates;
3. nested radicals;
4. reversible transformations;
5. logarithms as exponents;
6. integer / domain filters;
7. source audit.

## Production gates
Before readiness can be considered:
- independent solution of both historical anchors using corrected source custody;
- independent verification of every authored item key;
- explicit domain/reversibility notation throughout solutions;
- frozen 31-column metadata only;
- no learner-facing H/T/topic/wave labels;
- first mastery attempt unlabelled and unhinted;
- source-marker / metadata / teacher-key closure;
- canonical student and teacher PDFs with SHA-256, Git blob, page count, structural preflight and independent page-by-page visual QA.

Classroom timing/readability, retention, psychometric calibration, qualification probability and publication approval remain `NOT_RUN` until real evidence exists.

## Planned integrated learner package
1. Concept and Dependency Map
2. Source Coverage / correction-aware PYQ Map
3. Assimilation Book
4. First-Step Reference
5. Recognition and First-Line Lab
6. Practice / Transfer Bank with visible F0→F4 support fading
7. Mixed H0 Mastery Test with no method labels or hints
8. Teacher Diagnostic Key
9. Metadata / QA / PDF custody

This file establishes the authoring and mathematical safety boundary only; it does not claim source-audit completion.
