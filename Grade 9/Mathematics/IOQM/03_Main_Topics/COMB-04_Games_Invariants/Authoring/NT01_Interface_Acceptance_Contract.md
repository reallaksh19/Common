# COMB-04 <- NT-01 Interface Acceptance Contract

Provider: `IOQM-G9-NT-01`  
Pinned blob: `5212297212fb097cd508e9fc9d5848b271bc0ad1`  
Provider status: `FROZEN_V1_FOR_DOWNSTREAM_RETRIEVAL`  
Acceptance: `PASS`

## Retrieved only

COMB-04 may retrieve:

- parity/divisibility-style invariant language;
- divisibility meaning `d|x` and difference/linear-combination facts when needed as short proof bridges;
- ordinary integer arithmetic already in G9 core.

## Not imported as local canon

COMB-04 must not rebuild:

- Euclidean algorithm teaching;
- gcd/lcm reconstruction doctrine;
- equal-remainder/gcd-of-differences chapter sequences;
- generic divisibility chains.

## COMB-04-owned transformation

The topic may use parity/divisibility facts as raw invariant material, but it must teach the game/reachability question:

`what move effect is preserved -> how does it constrain adversarial/reachability outcomes?`

Provider downstream statement explicitly permits COMB-04 to retrieve divisibility/parity-style invariant language while retaining adversarial game/invariant strategy as COMB-04 canon.

`DEPENDENCY_INVERSION: NONE`  
`CANONICAL_DUPLICATION: NO`  
`DOWNSTREAM_ACCEPTANCE: PASS`