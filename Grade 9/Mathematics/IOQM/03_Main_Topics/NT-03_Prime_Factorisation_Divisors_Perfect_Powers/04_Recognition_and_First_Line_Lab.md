# Recognition and First-Line Lab

Write only the first useful mathematical object or line unless the item asks for a value.

1. `N=2^7*3^4*5^3`. First line for the number of square divisors.
2. `756` must be multiplied by the least positive integer to become a square. First line.
3. `M=2^18*3^12*5^6`. First line for the greatest `k` such that `M` is a perfect `k`-th power.
4. Decide whether `1764` has an odd or even number of divisors without listing them.
5. First line for `v_3(50!)`.
6. If `xy=2^6*3^4` and `gcd(x,y)=1`, state the structural restriction on the factors.
7. A positive integer has exactly 12 divisors. List the possible exponent patterns.
8. Explain in one sentence why divisors of `n^2` pair around `n`.
9. Which route is cheaper for `gcd(987654,123456)`: Euclid or full prime factorisation?
10. Which route is correct for "largest `k` such that `10^24*21^12` is a perfect `k`-th power"?
