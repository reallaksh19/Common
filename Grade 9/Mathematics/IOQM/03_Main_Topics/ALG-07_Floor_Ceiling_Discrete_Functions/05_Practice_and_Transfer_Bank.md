# ALG-07 - Practice and Transfer Bank

All items are author-created.

## F0 - Foundation

1. Evaluate `floor(4.6)`, `floor(-4.6)`, `ceil(4.6)`, `ceil(-4.6)`.
2. Translate `floor(x)=n` and `ceil(x)=n` into inequalities.
3. Find `{3.25}` and `{-2.3}`.
4. Verify `ceil(x)=-floor(-x)` for `x=2.4` and `x=-2.4`.

## F1 - Direct

5. Solve `floor(x)=4` over the reals.
6. Solve `ceil(x)=4` over the reals.
7. Solve `floor((x+2)/3)=2`.
8. Solve `floor(-x)=3`.
9. If `3<x<4` and `{x}=0.25`, find `x`.

## F2 - Standard

10. Solve `floor(2x+1)=5`.
11. Solve `ceil(3x-2)=4`.
12. Solve `floor(x)=ceil(x)`.
13. Solve `floor(x)+ceil(x)=7`.
14. Classify the value of `floor(x)+floor(-x)` for integer and non-integer `x`.
15. Solve `floor(x^2)=4`.

## F3 - Disguised

16. How many integers lie in `[-3.7,5.2)`?
17. How many integers `k` satisfy `floor((k+1)/3)=2`?
18. Solve simultaneously `floor(x)=2` and `floor(2x)=5`.
19. Explain why `ceil(x)=-floor(-x)` follows from the two interval definitions.
20. Characterize all real `x` such that `floor(x+1/2)=floor(x)`.

## F4 - Preliminary-style transfer

21. Can `floor((x-1)/2)=floor((x+1)/2)` ever hold? Prove your answer without case enumeration.
22. Solve `floor(x)+floor(x+1/2)=5`.
23. Find `x` if `floor(x)=3` and `{x}=0.7`.
24. If `floor(x)=-4`, state the full possible interval for `x` and explain why truncation can mislead.
25. Find all integers `n` satisfying `floor(n/4)=3`.
26. How many integers `n` satisfy `ceil(n/5)=3`?
27. For `x>0`, solve `floor(1/x)=2`.
28. Prove `floor(x+k)=floor(x)+k` for integer `k` from the interval definition.
29. WHY-NOT: why does `floor(x)=2` not allow the replacement `x=2`?
30. A gate is active for real times `t` in `[2.3,8.0)`. How many integer timestamps occur while it is active?
