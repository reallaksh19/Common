name: Primary learning semantics v1

on:
  pull_request:
    paths:
      - 'Primary/Architecture/**'
      - 'skills/primary-teacher-runtime/**'
      - 'skills/grade4-english/**'
      - 'skills/grade4-math/**'
      - '.github/workflows/primary-learning-semantics-v1.yml'
  push:
    branches: [main]
    paths:
      - 'Primary/Architecture/**'
      - 'skills/primary-teacher-runtime/**'
      - 'skills/grade4-english/**'
      - 'skills/grade4-math/**'
      - '.github/workflows/primary-learning-semantics-v1.yml'

permissions:
  contents: read

jobs:
  validate-primary-semantics:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate canonical Primary semantic fixture
        run: python Primary/Architecture/contracts/v1/validate_semantics.py

      - name: Validate diagnostic reasoning companion
        run: python Primary/Architecture/contracts/v1/validate_diagnostic_reasoning.py

      - name: Validate Grade 4 subject skill wiring
        run: python Primary/Architecture/contracts/v1/validate_grade4_skill_wiring.py
