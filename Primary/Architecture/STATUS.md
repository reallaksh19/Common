# Status

Current phase: **Common semantic authority v1 candidate**.

Tracking: Common #162.

The architecture is intentionally being frozen in Common before Study-Hub Phase 2 transport contracts are finalized.

Next actions after review/merge:

1. wire Grade 4 Math and English skills to `primary-teacher-runtime` for interactive tutoring;
2. refactor Study-Hub PR #51 into transport/adapters that consume Common semantics;
3. extend `kani-attempt-v1` only with bounded observable Primary evidence needed by the app boundary;
4. run the Grade 4 fractions cross-renderer vertical slice.
